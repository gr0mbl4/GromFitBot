#!/usr/bin/env python3
"""
Точка входа для запуска бота с правильными путями импорта
"""

import sys
import os

# Получаем абсолютный путь к корневой директории проекта
current_dir = os.path.dirname(os.path.abspath(__file__))

# Добавляем корневую директорию проекта в путь Python
sys.path.insert(0, current_dir)

print(f"📁 Рабочая директория: {current_dir}")
print(f"🐍 Python path: {sys.path[0]}")

try:
    # Проверяем наличие виртуального окружения
    venv_path = os.path.join(current_dir, 'venv')
    if os.path.exists(venv_path):
        print("✅ Виртуальное окружение найдено")
    else:
        print("⚠️ Виртуальное окружение не найдено")
    
    # Импортируем и запускаем основную логику
    from src.main import main
    
    print("🚀 Запуск GromFitBot...")
    import asyncio
    asyncio.run(main())
    
except ImportError as e:
    print(f"❌ Ошибка импорта: {e}")
    print("\n🔧 Возможные решения:")
    print("1. Убедитесь, что все файлы __init__.py созданы")
    print("2. Проверьте наличие файла src/__init__.py")
    print("3. Убедитесь, что установлены все зависимости: pip install -r requirements.txt")
    sys.exit(1)
except Exception as e:
    print(f"❌ Ошибка при запуске: {e}")
    sys.exit(1)